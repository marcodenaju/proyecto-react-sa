import { createTheme } from "@mui/material";

export const customTheme = createTheme({
    palette: {
        primary: {
            main: "#D87D4A",
            secondary: "#F1F1F1"

        },
        secondary: {
            main: "#101010"
        }
    }

})